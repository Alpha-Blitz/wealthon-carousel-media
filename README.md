# Wealthon Carousel Media

Public-hosted slide images for **The Capital Series** carousels, served via
`raw.githubusercontent.com` so Buffer can pull them into Instagram + LinkedIn
draft posts (Buffer has no media upload and needs direct public URLs).

Layout: `Post-NN/slide_NN.png`.
